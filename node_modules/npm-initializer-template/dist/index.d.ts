export interface CreateProjectOpts {
    npmPackage: {
        name: string;
        version?: string;
    };
    workingDirectory: string;
}
export declare function createProject(opts: CreateProjectOpts): Promise<boolean>;
