"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
const execute_1 = require("./utils/execute");
const workingDirectory = './test-repository-index';
test('Should create a project from an NPM template', async () => {
    const config = {
        workingDirectory,
        npmPackage: {
            name: '@edwas/common',
        },
    };
    const result = await _1.createProject(config);
    expect(result).toBe(true);
    await execute_1.execute('.', `rm -rf ${workingDirectory}`);
});
//# sourceMappingURL=index.spec.js.map